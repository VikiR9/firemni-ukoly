NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_your_key
# Standard Web Push. Generate once and retain the same key pair across deploys.
# The public key is returned to authenticated devices by /api/push/subscribe.
PUSH_VAPID_PUBLIC_KEY=
PUSH_VAPID_PRIVATE_KEY=
PUSH_VAPID_SUBJECT=https://firemni-ukoly.vercel.app
PUSH_WORKER_SECRET=

# Server-only application gateway
ATTENDANCE_GATEWAY_SECRET=
# Passwords are individual hashes in attendance_private.accounts.
